import { ApellidoPipe } from './nombreYapellido.pipe';

describe('ApellidoPipe', () => {
  it('create an instance', () => {
    const pipe = new ApellidoPipe();
    expect(pipe).toBeTruthy();
  });
});
